export enum PagesUrl {
  HOME = "/",
  AUTH = "/auth",
  CATEGORIES = "/categories",
  RECIPES = "/recipes",
  RANDOM = "/random",

  //private routes
  RECIPE_BOOK = "/i/recipe-book",
  PROFILE = "/i/profile",
}
